# OpenAI API Key (Required)
OPENAI_API_KEY=your_openai_api_key_here

# PostgreSQL Database (Optional - only needed for sharing feature)
# If using docker-compose, these will be set automatically
# If using external database, provide the full connection string:
POSTGRES_URL=postgresql://username:password@host:port/database_name

# PostgreSQL settings (only used with docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=openai_fm
